'use strict';

/**
 * @ngdoc function
 * @name crawlerApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the crawlerApp
 */
angular.module('crawlerApp')
  .controller('AboutCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
